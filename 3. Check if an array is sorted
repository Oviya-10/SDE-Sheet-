public class Solution {
    public static int isSorted(int n, int []arr) {
        for(int i=0;i<n-1;i++)
        {
            if(arr[i+1]<arr[i])
              return 0;
        }
        return 1;
    }
}
